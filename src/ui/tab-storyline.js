/**
 * tab-storyline.js - Storyline Manager UI
 * Renders storyline list, scene/chapter nodes, progress bar.
 */
export function renderStorylineTab(containerEl) {
    containerEl.innerHTML = '<p style="padding:1em;color:var(--SmartThemeBodyColor)">故事线管理 — 开发中</p>';
}
