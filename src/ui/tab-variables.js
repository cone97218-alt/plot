/**
 * tab-variables.js - Variables/World State UI
 * Renders variable list, add/edit/delete controls, AI generate button, badge notification.
 */
export function renderVariablesTab(containerEl) {
    // TODO: render full variables tab UI
    containerEl.innerHTML = '<p style="padding:1em;color:var(--SmartThemeBodyColor)">变量系统 — 开发中</p>';
}
